Engraved mono button — the one action primitive; every clickable command uses it.

    <Button>Trace</Button>
    <Button variant="solid">Next ›</Button>

Variants: outline (default), solid (inverted ink — the primary action), ghost. Sizes sm / md / lg. Labels render uppercase; use unicode glyphs (❚❚ ▶ ⌖ ✕ ‹ ›), never icon fonts.